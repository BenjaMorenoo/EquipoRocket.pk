# Frontend Environment Variables

# API Gateway URL (centralized entry point for all microservices)
# When running with Docker Compose, use the gateway service name
VITE_API_URL=http://api_gateway:9000

# For local development (without Docker)
# VITE_API_URL=http://localhost:9000
